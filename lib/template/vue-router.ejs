import { RouteRecordRaw } from "vue-router";
const router: RouteRecordRaw = {
  path: "/<%= data.lowerName %>",
  name: "<%= data.name %>",
  component: () => import("./<%= data.name %>.vue")
};
export default router;
